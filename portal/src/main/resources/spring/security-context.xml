<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/pages/admin/**" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/pages/company/**" access="hasRole('ROLE_COMPANY')" />
        <intercept-url pattern="/pages/customer/**" access="hasRole('ROLE_CUSTOMER')" />
        <intercept-url pattern="/pages/error/**" access="permitAll" />
        <intercept-url pattern="/" access="hasAnyRole('ROLE_ADMIN', 'ROLE_COMPANY', 'ROLE_CUSTOMER')" />

        <form-login
                login-page="/login"
                default-target-url="/"
                login-processing-url="/auth"
                authentication-failure-url="/login?error"
                username-parameter="username"
                password-parameter="password" />
        <logout logout-url="/logout" logout-success-url="/" />
    </http>

    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.password.NoOpPasswordEncoder"/>

    <authentication-manager>
        <authentication-provider >
            <user-service>
                <user name="test" password="test" authorities="ROLE_COMPANY" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>